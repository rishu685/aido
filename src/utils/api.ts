// This file is kept for compatibility but no longer used for authentication
// The app now works without tRPC and authentication
export const api = {
  withTRPC: (component: any) => component,
};

export type RouterInputs = any;
export type RouterOutputs = any;